---
import Header from "./Header.astro";
import Footer from "./Footer.astro";
import "../styles/global.css";
import { ViewTransitions } from "astro:transitions";
import LevitationMenu from "@components/LevitationMenu";
import SearchPalette from "@components/SearchPalette";
const { title } = Astro.props;
const { pathname } = Astro.locals;
const cwd = pathname === "/" ? "~" : "~" + pathname.replace(/\/$/, "");
const displayTitle = title || "krismile.blog";
---

<html lang="zh-cn" class="dark">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="RSS"
      href="/rss.xml"
    />
    <link rel="manifest" href="/mainfest.json" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="krismile.blog"
      href={new URL("rss.xml", Astro.site)}
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{displayTitle}</title>
    <ViewTransitions />
  </head>
  <body>
    <div class="site-backdrop" aria-hidden="true">
      <div class="site-glow site-glow-a" data-motion="glow"></div>
      <div class="site-glow site-glow-b" data-motion="glow"></div>
    </div>
    <div class="min-h-screen flex flex-col site-shell" data-motion-root>
      <Header cwd={cwd} />

      <main class="flex-grow w-full max-w-6xl mx-auto px-4 sm:px-6 lg:px-10 pt-24 sm:pt-28 pb-16">
        {title && (
          <header class="page-title" data-motion="panel">
            <div class="text-base text-term-text-muted mb-2">
              <span class="term-prompt">$</span> cat
              <span class="ml-2 text-term-text-secondary">{(title + ".md").toLowerCase().replace(/\s+/g, "-")}</span>
            </div>
            <h1 class="text-3xl sm:text-4xl md:text-[2.5rem] font-semibold text-term-text-primary">
              {title}
            </h1>
          </header>
        )}
        <slot />
      </main>

      <div class="fixed bottom-6 right-4 sm:hidden z-50">
        <LevitationMenu client:load />
      </div>

      <SearchPalette client:idle />

      <Footer />
    </div>
    <script>
      import "../scripts/click-effect.js";
      if ("serviceWorker" in navigator) {
        navigator.serviceWorker.register("/sw.js");
      }
    </script>
    <script>
      import "../scripts/terminal-motion";
    </script>
  </body>
</html>

<style is:global>
  .text-term-accent { color: var(--accent); }
  .text-term-accent-soft { color: var(--accent-soft); }
  .text-term-accent-dim { color: var(--accent-dim); }
  .text-term-text-primary { color: var(--text-primary); }
  .text-term-text-secondary { color: var(--text-secondary); }
  .text-term-text-muted { color: var(--text-muted); }
  .text-term-text-faint { color: var(--text-faint); }
  .border-term-border-default { border-color: var(--border-default); }
  .border-term-border-subtle { border-color: var(--border-subtle); }
  .border-term-border-strong { border-color: var(--border-strong); }
  .border-term-accent-dim { border-color: var(--accent-dim); }
  .bg-term-bg { background-color: var(--bg); }
  .bg-term-surface { background-color: var(--surface); }
  .bg-term-elevated { background-color: var(--elevated); }
</style>
