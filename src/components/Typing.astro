<div class="absolute top-0 left-0 flex items-center justify-center h-full w-full cursor-pointer -z-10">
  <div class=" relative text-center mt-10 w-[34ch]">
    <p id="typing"></p>
  </div>
</div>

<style>
  :root {
    --pointerColor: #1BEABD;
  }
  #typing {
    display: inline;
    font-size: 32px;
    background: linear-gradient(
      90deg,
      transparent,
      transparent calc(100% - 2px),
      var(--pointerColor) calc(100% - 2px),
      var(--pointerColor)
    );
    animation: colorChange 0.8s linear infinite;
    padding-right: 4px;
  }
  @keyframes colorChange {
    0%,
    50% {
      --pointerColor: #1BEABD;
    }
    50%,
    100% {
      --pointerColor: transparent;
    }
  }
</style>

<script is:inline lang="ts" type="module">
  const text = `
  今日江头两三春，
  可怜和叶度残春。
  `;
  console.log("🚀 ~ text:", text)
  const contentElement = document.getElementById("typing");
  let index = 0;

  function addNextCharacter() {
    if (index < text.length) {
      contentElement.textContent += text[index];
      index++;
      setTimeout(addNextCharacter, Math.random() * 150 + 30);
    }
  }

  addNextCharacter();
</script>
